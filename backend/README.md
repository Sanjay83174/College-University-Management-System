# North Gate University Backend

## Setup

1. Install dependencies:
   ```
   npm install
   ```
2. Start the server:
   ```
   npm start
   ```

## API Endpoints

- `POST /api/admissions` — Submit an admissions application (expects JSON: `{ name, email, course }`)

## Next Steps
- Connect to MySQL and save form data
- Add more endpoints for other features 